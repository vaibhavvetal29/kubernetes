structure:
  the kubernetes object yaml file required 4 main keys
  Apiversion: You have to provide api version in this 
    example: v1,apps/v1
  Kind: the kind of object you wan to create
   example: pod,deployment,replicaset,service
  Metadata: The information about the object you are trying to create like name, labels, annotations
  Spec: the specification of the desired state of an object

for more details : https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md


apiVersion:
kind:
metadata:
spec:
